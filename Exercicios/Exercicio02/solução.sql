SELECT "full_name"
FROM "[Sua Tabela]"
WHERE full_name = LOWER(full_name)
ORDER BY full_name ASC

-- Lower serve para deixar todas as letras em minusculo, você pode saber mais sobre funções de texto na pasta tutoriais.
